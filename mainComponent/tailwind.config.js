/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      // Tailwind будет искать классы во всех ваших .tsx файлах в папке src
      "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
